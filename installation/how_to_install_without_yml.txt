Note: download and use Python 3.8
In anaconda prompt: (do not type the ">")

Create the environment: conda create --name dagws

Activate the environment: conda activate dagws

Change folder to the folder containing the file "requirements.txt" (using cd)

Install the packages using pip: pip install -r requirements.txt
